apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wordpress-phpmyadmin-localhost
spec:
  ingressClassName: nginx
  rules:
    - host: test.local
      http:
        paths:
          - backend:
              service:
                name: wordpress-phpmyadmin
                port:
                  number: 81
            path: /
            pathType: Prefix
